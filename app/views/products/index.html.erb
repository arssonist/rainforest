<h1>Products#index</h1>

  <p>RainForest</p>

<%= link_to "New Product", new_product_path %>


<p>The first argument is the name of the partial we want to render, leaving out the leading underscore.</p>

<p>This is the left-hand side (the key) of the hash we are passing. The right-hand side (the value) is the location object itself.
</p>

<%= render 'product', product: @products %>
